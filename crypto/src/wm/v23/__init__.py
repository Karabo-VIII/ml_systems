"""V23 — xLSTM (Beck et al., NeurIPS 2024).

Extended LSTM with exponential gating + matrix memory. Recurrent SOTA
alternative to V6's GRU JEPA backbone, with linear-in-T compute cost.

Status: BACKBONE SCAFFOLD. Forward + backward + smoke verified.
"""
