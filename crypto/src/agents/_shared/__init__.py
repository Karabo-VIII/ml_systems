# _shared -- genuinely class-agnostic helpers shared across A1/A2/A1H.
# Class-specific code stays in its class dir; only truly agnostic utilities
# (generic policy/reward building blocks) belong here.
