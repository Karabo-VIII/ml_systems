<!-- ideation workflow: 64 edges, 10 agents, ranked. /orc 2026-06-10 -->

I have everything I need. The critic analysis is thorough and verified against the dead-list, tooling, and data tree. I'll synthesize the final ranked hit-list per the requested format.

# Final Ranked Hit-List — Long-Only Spot No-Leverage Crypto Book

Scored on **(EV if real) × P(held-out positive after cost) × (cheapness to backtest on data in hand)**. Ranked descending. All tooling referenced (`src/strat/{mover_capture,dual_null_evaluator,candidate_gate,pbo_cscv,setup_harness,battery}.py`) exists; sub-hour data (`data/processed/chimera/{15m,30m}/` = 104 assets, `data/raw/<SYM>/klines_1m/`) exists — so every "blocked on sub-hour data" claim in the inputs is FALSE and those tests are runnable today.

| # | Edge | Entry / Exit / Hold | Data needed | One-line backtest | P(held-out +) |
|---|------|---------------------|-------------|-------------------|----------------|
| **1** | **Vol-targeting SIZING overlay** (on a base long basket) | Hold EW basket continuously; size = target_vol / HAR-RV forecast, capped 1.0, floored at min. Never fully exit; resize daily. | price-only (vol features `norm_yz_volatility`, `rv_rv_5m` already in chimera) | Wrap any base-long backtest in HAR-RV inverse-vol sizing; compare compound + maxDD + Calmar vs unsized at taker cost (`battery.py`). | **~0.85** — but as Calmar/DD shaper, NOT alpha; upside capped under no-lev |
| **2** | **Cross-sectional LOW-realized-vol tilt** (low-vol premium) | Monthly rebalance: rank survivorship-clean universe by trailing 30-60d RV; EW-long the bottom-vol quintile. Exit = next rebalance when a name leaves the quintile. Hold weeks-months. | price-only OHLCV + **survivorship-clean universe** (load-bearing) | Bottom-vol-quintile EW vs EW-buy&hold and top-vol-quintile, full-cycle Calmar incl. a bear window, OOS→UNSEEN. | **~0.55** — contested lit; likely defensive-beta tilt, fades in bear |
| **3** | **Connors RSI(2) dip-buy, >200dMA + range-gated** (E1 MR) | Daily. Enter next-bar: close>SMA200 AND NOT down-trend (`norm_efficiency` low / `hurst_regime`≥0.5) AND RSI(2)<10. Exit: RSI(2)>70 OR close>SMA5, time-stop 5 bars. Hold 1-5d. | price-only (daily OHLCV) | `setup_harness.py` setup → `dual_null_evaluator.py` (cost-matched random-entry-in-same-regime null) → `candidate_gate.py` @ 0.0024 → `pbo_cscv.py` on threshold grid. | **~0.35** — marginal on BTC/ETH; likely fails 10-seed + UNSEEN-alts |
| **4** | **CFTC COT institutional-positioning regime-conditioner** (the missed edge) | Weekly: when CME large-trader net-long z rises through threshold (3d publication lag, A6-safe), scale book exposure up; cut when it falls. Exposure multiplier, not a bar signal. Hold = regime. | external (CFTC COT, **FREE** via public API / `cot_reports`, ~1-2d ingest) | Ingest COT, build weekly net-long z; A/B exposure-gated book vs ungated on full-cycle Calmar, OOS→UNSEEN. | **~0.45** — cleanest leading profile; likely preservation>alpha but zero dead-list collision |
| **5** | **Listing-survivor quality + vol-target overlay PAIR** (E7+E10) | Pre-filter ANY small-cap basket: include a name only if it passes min-ADV + min-age + tier-1-listed + concentration sanity; EW survivors; drop on liquidity-gate break. Inherits host hold. | external (ADV, listing tier, token-age, concentration) + price-only | Apply filter to an EW basket; compare compound + maxDD + worst-trade with vs without, on survivorship-clean panel. | **~0.70** — real as tail-risk reducer (raises compound by avoiding −90% names); not alpha |
| **6** | **New-listing momentum sleeve (P8 / Approach-20)** — the documented survivor | Buy first fully-formed bar AFTER tier-1 listing (no same-bar fill) IF first 1-3 bars net-positive momentum AND first-hour-$-vol floor passes. Exit: fixed 1-3d OR trail; HARD dump time-stop. | external (listing-event feed; `listing_dates.py` + `multi_venue_listings.py` exist) + price-only | RE-TEST existing P8 logic through current `candidate_gate` at taker 0.0024 with strict first-hour-$-volume floor; compare to shipped OOS. | **~0.40** — known 5.76× live attenuation (thin books, maker p_fill 0.21-0.40); marginal-positive |
| **7** | **Funding-extreme contrarian CONDITIONER, range-gated** (E5 — gate only) | 4h/daily majors. Enter next-bar: range-confirmed AND close>SMA200 AND `fund_rate_z30`<−1.5 AND an E1/E3 oversold trigger fires. Exit: funding normalizes OR revert to SMA20; time-stop 3-6 bars. | chimera-structural (`fund_rate_z30`, majors-only — 100% null on memes) + price-only | `setup_harness.py` funding-z ANDed onto E1; ablate marginal contribution on UNSEEN; **verify sign vs D42 first** (funding sign inverts at our cadence). | **~0.30** — must independently lift a base metric on UNSEEN or collapses into E1 |
| **8** | **rv_skew_5m / rv_kurt_5m magnitude-asymmetry feature → re-mine breakouts** (the missed feature) | Build causal realized-skew from existing 5m pipeline; use skew>0 as the directional gate on a BB-squeeze / range-expansion breakout (the one feature that attacks the "vol-expansion = coin-flip direction" problem). | price-only (5m pipeline exists; feature not yet built, ~1d) | Build `rv_skew_5m`; re-run the breakout family WITH it through `mover_capture.py` + `dual_null_evaluator.py` before declaring the cluster dead. | **~0.30** — the un-built shape feature that could break the coin-flip; honest unknown |
| **9** | **Hour-of-day overnight tilt (22:00 UTC, 2h hold)** (C1 calendar) | Buy spot BTC 22:00 UTC (sweep 19-23 UTC on TRAIN, lock on VAL), sell 00:00 UTC. Daily-repeating. Variant: extend to 4h to amortize cost. | price-only (**native** hourly OHLCV — not dollar-bar-derived) | Group hourly BTC by UTC hour, mean fwd-2h return + block-bootstrap CI; survivors → `battery.py` with MakerCostModel (p_fill 0.25-0.50) on UNSEEN. | **~0.25** — binding question is COST not signal (gross ≈ one taker RT); thin-positive at maker only |
| **10** | **Post-token-unlock relief bounce, exhaustion-gated** (E3 unlock) | On unlock-date+K (24-72h post, after recipients liquidate), large cliff unlocks (≥1-5% supply) only, if sell-pressure decelerating: buy. Exit: fixed 3-10d OR reclaim of pre-unlock level. | external (unlock calendar — Tokenomist/TokenUnlocks, FREE) + price-only | Event-study: entry at unlock+{1,2,3,5}d held {3,5,10}d vs cost-matched random null AND vs **D61 no-skill fixed-hold control** (mandatory). | **~0.25** — D48/D49 anti-edge cousin UNLESS entry strictly post-exhaustion; relief-LONG leg inferred not evidenced |
| **11** | **Intraday mover-selection × hour-of-day conditioning** (D68 × C1 — the missed combination) | 1h: rank universe by trailing few-hour return + volume surge; buy top-N **ONLY inside documented high-skew UTC windows**; short fixed hold (hours), hard time-stop. | price-only (15m chimera, 104 assets, exists today) | `mover_capture.py --entry-kind momentum` at 1h gated to UTC windows; cost-matched null + **UNSEEN persistence + `beats_rnd_daily`** (D68's failure point). | **~0.20** — attacks D68's exact failure mode (few-big-days); 1h cost wall (D60) is the killer |
| **12** | **Index/ETF-rebalance forced-buy front-run** (E6-rebalance event) | On rebalance announcement / record-date (additions published), buy the added asset; exit at/just before rebalance execution (sell-the-event), hard time-stop at rebalance timestamp. Hold ~1 week. | external (Bitwise/FTSE rebalance notices, FREE, scheduled) + price-only | Event-study: entry at record-date held to rebalance vs cost-matched null; pool across index providers for n. | **~0.20** — cleanest/leak-free but likely too small (thin crypto-index AUM; equity index-effect arbitraged to ~0) |

---

## The single highest-EV thing to build FIRST

**Build the vol-targeting sizing overlay (#1) — but only as the wrapper, and pair it immediately with the low-vol cross-sectional tilt (#2) as its base.**

Why this, decisively:

1. **It is the only item with no dead-list collision and a P(works) above 0.8.** D02 refutes voladj *targets*, not *sizing* — the corpus shortlist independently ranks it the single least-contested win. Everything else in the table is either marginal-after-cost (#3, #7, #9), data-gated (#4, #6, #10, #12), or a defensive layer (#5).

2. **It is the cheapest possible test.** ~0.5 file. Vol features (`norm_yz_volatility`, `rv_rv_5m`) are already in every chimera parquet. No ingestion, no new universe, no event feed. RWYB today.

3. **It is the correct objective.** The mandate is robust held-out *compound* under LO+no-lev+DD<30%. Vol-targeting is the one operation that directly improves robust Calmar/maxDD on whatever it scales — and under no-leverage its value is asymmetric downside-protection (it can't lever up in calm vol, so it shapes the loss profile rather than juicing return). That is exactly the wealth-and-robustness objective, not Sharpe-chasing.

4. **It de-risks the entire portfolio thesis.** The honest conclusion this set forces is that no single edge here clears +1%/day robustly (D44: that needs Sharpe≈5 / IC≈0.6 at daily bars — math-infeasible). The deployable answer is **stack overlays (#1+#5) on slow tilts (#2) and event sleeves (#6) plus the free leading conditioner (#4)** — a disciplined-beta + preservation + thin-event-alpha book. #1 is the load-bearing first brick of that stack: it is what makes every other sleeve *survivable* at DD<30%, so it must exist before any of them is sized into capital.

**Honest ceiling, stated plainly:** this book is disciplined-beta + preservation + thin-event-alpha. +1%/day robustly is NOT in this candidate set. Any number that looks like it hits the target from these edges is in-sample, un-deflated, or survivorship-inflated — verify against `candidate_gate` + 10-seed + UNSEEN before believing it.