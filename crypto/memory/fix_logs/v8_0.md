# V8.0 Fix Log

## 2026-04-07: Runner f25 taxonomy mismatch
**Severity:** Medium (argparse reject at runtime, would block f25 slot in tournament)
**Files:** `src/run_all_training.py`
**Commit:** 8371b2f

**Problem:** `run_all_training.py` listed V8 as supporting features `[13, 18, 25, 34]`, but V8 settings.py only defines FEATURE_LIST_{13,18,30,34,37,41}. f25 was a copy-paste artifact from V11-V14 (hand-picked 3-way taxonomy). V8 lives in the 41-feature SOTA prefix-slice taxonomy where f25 is not a defined slice.
**Fix:** Runner now declares V8 as `[13, 18, 34]`.
**Adversarial note:** V8 does NOT have a `--clean` variant (no world_model_clean.py), so it escaped the log_vars IndexError bug that hit V3/V6/V9. V8's 7-element RSSM log_vars layout (LOG_VAR_INIT in settings.py line 312) matches the hardcoded indices in train_world_model.py:587-596.

---

## 2026-03-18: Full preflight audit (cross-version invariants)
**Severity:** Critical (multiple training failures prevented)
**Files:** 16 files across V8 base + 3 variants

### CRITICAL-1: DIRECT_RETURN_WEIGHT = 3.0
**Files:** All 4 settings.py
**Fix:** Changed to 1.0. Huber shortcut bypasses TwoHot bottleneck.

### CRITICAL-2: WM_STEPS_PER_EPOCH = 400
**Files:** All 4 settings.py
**Fix:** Changed to 2000. 400 = 5x less data exposure, ShIC checks fire before model learns.

### CRITICAL-3: DIVERSITY_STEPS_PER_EPOCH = 400
**Files:** All 4 settings.py
**Fix:** Changed to 2000. NCL diversity undertraining.

### CRITICAL-4: Missing strict=False on load_state_dict
**Files:** `train_world_model.py` (model + ema_model load_state_dict calls)
**Fix:** Added `strict=False` to both calls.

### CRITICAL-5: shic_decline_count not persisted
**Files:** `train_world_model.py` (save + load_latest)
**Fix:** Added to checkpoint state dict and load_latest return (5-tuple -> 6-tuple).

### CRITICAL-6: Missing n_features + collision guard
**Files:** `train_world_model.py`
**Fix:** Added n_features to checkpoint, collision guard in load_latest, --features argparse.

### CRITICAL-7: TwoHotSymlog defaults [-5, 5]
**Files:** All 4 components.py
**Fix:** Changed to [-1, 1]. If constructed without explicit args, would produce wrong bins.

### HIGH-1: Feature config plumbing
**Files:** `train_world_model.py`
**Fix:** Added `get_feature_config(n_features)` call, `input_dim` param to model/RevIN/autopsy.

### HIGH-2: ATME strength params too weak
**Files:** All 4 settings.py
**Fix:** TEMPORAL_CTX_DROP 0.15 -> 0.40, SEQ_SHUFFLE_PROB 0.20 -> 0.30. Partial mitigation for z_post leakage.
**Note:** V8 still has ATME z_post leakage pattern in world_model.py (temporal info leaks through posterior before ATME zeros h_seq). Full fix deferred to training time due to architecture complexity.

### MEDIUM-1: Stale argparse choices
**Files:** 3x train_adapter.py, 3x train_ncl.py, 3x train_snapshot.py, 3x validate_world.py
**Fix:** choices=[13, 18] -> [13, 18, 30, 37]; validate default=22 -> default=13.

### MEDIUM-2: Stale vol-adjusted comments
**Files:** All 4 settings.py
**Fix:** "vol-adjusted targets" -> "Raw return targets"; ATME comment -> "Obs-only posterior"
